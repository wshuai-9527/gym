(()=>{
  'use strict';
  const KEY='gym-vault-v36-cloud';
  const YEAR=2026;
  const V='V37';
  const mainExercises=['平板卧推','坐姿推肩','上斜卧推','高位下拉','上斜划船','双臂哑铃俯身划船','罗马尼亚硬拉','保加利亚分腿蹲','靠腿二头弯举'];
  const catalog={
    '平板卧推':{module:'Push',muscles:['胸','前三角','三头'],pattern:'水平推',multi:2},'上斜卧推':{module:'Push',muscles:['上胸','前三角','三头'],pattern:'上斜推',multi:2},'坐姿推肩':{module:'Push',muscles:['肩','三头','核心'],pattern:'垂直推',multi:2},'侧平举':{module:'Push',muscles:['中束'],pattern:'肩外展',multi:2},'过顶臂屈伸':{module:'Push',muscles:['三头'],pattern:'肘伸',multi:2},'俯卧撑':{module:'Push',muscles:['胸','前三角','三头'],pattern:'自重推',multi:1},
    '高位下拉':{module:'Pull',muscles:['背阔肌','二头'],pattern:'垂直拉',multi:1},'上斜划船':{module:'Pull',muscles:['中背','背阔肌','后束'],pattern:'水平拉',multi:2},'单臂哑铃划船':{module:'Pull',muscles:['背阔肌','中背','二头'],pattern:'单侧水平拉',multi:1},'双臂哑铃俯身划船':{module:'Pull',muscles:['中背','背阔肌','后束'],pattern:'髋折叠划船',multi:2},'上斜宽肘划船':{module:'Pull',muscles:['后束','上背'],pattern:'宽肘划船',multi:2},'坐姿俯身飞鸟':{module:'Pull',muscles:['后束','上背'],pattern:'肩水平外展',multi:2},'哑铃仰卧上拉':{module:'Pull',muscles:['背阔肌','胸'],pattern:'肩伸',multi:2},'靠腿二头弯举':{module:'Pull',muscles:['二头'],pattern:'肘屈',multi:2},
    '壶铃摆动':{module:'Legs',muscles:['臀','后链','核心'],pattern:'髋爆发',multi:1},'保加利亚分腿蹲':{module:'Legs',muscles:['股四头','臀','稳定'],pattern:'单腿蹲',multi:2},'原地箭步蹲':{module:'Legs',muscles:['股四头','臀','稳定'],pattern:'弓步',multi:2},'罗马尼亚硬拉':{module:'Legs',muscles:['后链','臀','腘绳肌'],pattern:'髋铰链',multi:2},'平板支撑':{module:'Legs',muscles:['核心'],pattern:'抗伸展',multi:1}
  };
  const alias={'哑铃平板卧推':'平板卧推','标准俯卧撑':'俯卧撑','窄距俯卧撑':'俯卧撑','哑铃坐姿推肩':'坐姿推肩','哑铃推肩':'坐姿推肩','哑铃侧平举':'侧平举','哑铃过顶臂屈伸':'过顶臂屈伸','坐姿臂屈伸':'过顶臂屈伸','臂屈伸':'过顶臂屈伸','双哑铃上斜划船':'上斜划船','上斜哑铃划船':'上斜划船','单臂划船':'单臂哑铃划船','哑铃划船':'单臂哑铃划船','俯身反向飞鸟':'坐姿俯身飞鸟','俯身飞鸟':'坐姿俯身飞鸟','俯身哑铃飞鸟':'坐姿俯身飞鸟','二头弯举':'靠腿二头弯举','靠腿弯举':'靠腿二头弯举','坐姿靠腿二头弯举':'靠腿二头弯举','哑铃二头弯举':'靠腿二头弯举','保加利亚蹲':'保加利亚分腿蹲','哑铃箭步蹲':'原地箭步蹲','箭步蹲':'原地箭步蹲','哑铃罗马尼亚硬拉':'罗马尼亚硬拉'};
  const $=s=>document.querySelector(s);
  const $$=s=>Array.from(document.querySelectorAll(s));
  const fmt=n=>Number(n||0).toLocaleString('zh-CN',{maximumFractionDigits:0});
  const pct=n=>(n>=0?'+':'')+Math.round(n*100)+'%';
  const clamp=(x,a,b)=>Math.max(a,Math.min(b,x));
  function canon(n){n=(n||'').trim();return alias[n]||n}
  function mdDate(s){const [m,d]=String(s).split('.').map(Number);return new Date(YEAR,m-1,d)}
  function label(d){return (d.getMonth()+1)+'.'+d.getDate()}
  function isoKey(d){return d.toISOString().slice(0,10)}
  function daysBetween(a,b){return Math.round((mdDate(b)-mdDate(a))/86400000)}
  function setParts(s){s=String(s||'').replace(/×/g,'*').replace(/\s/g,'');const nums=(s.match(/[\d.]+/g)||[]).map(Number);if(/kg/i.test(s)){return {kg:nums[0]||0,reps:nums[1]||0,count:nums[2]||1,seconds:null}}if(/s/i.test(s)){return {kg:0,reps:0,count:nums[1]||1,seconds:nums[0]||0}}return {kg:0,reps:nums[0]||0,count:nums[1]||1,seconds:null}}
  function dayCat(r){const names=(r.items||[]).map(i=>canon(i.name)).join(' ');if(names.includes('篮球'))return'Basketball';if(/休息|休整/.test(names))return'Rest';const score={Push:0,Pull:0,Legs:0};(r.items||[]).forEach(i=>{const c=catalog[canon(i.name)];if(c)score[c.module]++});return Object.entries(score).sort((a,b)=>b[1]-a[1])[0]?.[1]>0?Object.entries(score).sort((a,b)=>b[1]-a[1])[0][0]:'Other'}
  function getRecords(){try{const raw=JSON.parse(localStorage.getItem(KEY)||'{}');return (raw.records||[]).map(r=>({date:r.date,items:(r.items||[]).map(i=>({name:canon(i.name),sets:i.sets||[]}))})).sort((a,b)=>mdDate(a.date)-mdDate(b.date))}catch{return []}}
  function expand(records){const rows=[];records.forEach(r=>{const module=dayCat(r);(r.items||[]).forEach(it=>{const name=canon(it.name),c=catalog[name]||{module,muscles:['未分类'],pattern:'未分类',multi:1};(it.sets||[]).forEach(raw=>{const p=setParts(raw),count=Math.max(1,Math.round(p.count||1));for(let k=0;k<count;k++){const vol=(p.kg||0)*(p.reps||0)*(c.multi||1);rows.push({date:r.date,dateObj:mdDate(r.date),module:module==='Other'?c.module:module,name,kg:p.kg||0,reps:p.reps||0,seconds:p.seconds||0,volume:vol,e1rm:p.kg&&p.reps?p.kg*(1+p.reps/30):0,multi:c.multi||1,muscles:c.muscles,pattern:c.pattern})}})})});return rows}
  function calendar(records,rows){if(!records.length)return[];const first=mdDate(records[0].date),last=mdDate(records.at(-1).date),by={};records.forEach(r=>by[isoKey(mdDate(r.date))]={date:r.date,module:dayCat(r),volume:0});rows.forEach(x=>{const k=isoKey(x.dateObj);if(!by[k])by[k]={date:x.date,module:x.module,volume:0};by[k].volume+=x.volume});const out=[];for(let d=new Date(first);d<=last;d.setDate(d.getDate()+1)){const k=isoKey(d),v=by[k]||{date:label(d),module:'Blank',volume:0};out.push({...v,dateObj:new Date(d)})}return out}
  function rolling(arr,n){return arr.map((_,i)=>{const a=arr.slice(Math.max(0,i-n+1),i+1);return a.reduce((s,x)=>s+x.volume,0)/a.length})}
  function prRows(rows){return mainExercises.map(name=>{const xs=rows.filter(r=>r.name===name&&r.e1rm>0).sort((a,b)=>a.dateObj-b.dateObj);if(!xs.length)return null;const first=xs[0],last=xs.at(-1),best=xs.reduce((a,b)=>b.e1rm>a.e1rm?b:a,xs[0]);return {name,first,best,last,gain:(best.e1rm/first.e1rm-1),currentGain:(last.e1rm/first.e1rm-1),sets:xs.length}}).filter(Boolean).sort((a,b)=>b.gain-a.gain)}
  function compute(){const records=getRecords(),rows=expand(records),cal=calendar(records,rows);const strength=records.filter(r=>!['Basketball','Rest'].includes(dayCat(r)));const counts={Push:0,Pull:0,Legs:0,Basketball:0,Rest:0,Other:0};records.forEach(r=>counts[dayCat(r)]=(counts[dayCat(r)]||0)+1);const loads={Push:0,Pull:0,Legs:0};rows.forEach(r=>{if(loads[r.module]!=null)loads[r.module]+=r.volume});const total=rows.reduce((s,x)=>s+x.volume,0);const last7=cal.slice(-7),prev7=cal.slice(-14,-7),last28=cal.slice(-28);const acute=last7.reduce((s,x)=>s+x.volume,0),chronic=Math.max(1,last28.reduce((s,x)=>s+x.volume,0)/4);const acwr=acute/chronic;const last7Sessions=last7.filter(x=>['Push','Pull','Legs'].includes(x.module)).length,last7Basket=last7.filter(x=>x.module==='Basketball').length;const lastRest=[...cal].reverse().find(x=>x.module==='Rest');const daysSinceRest=lastRest?Math.max(0,Math.round((cal.at(-1).dateObj-lastRest.dateObj)/86400000)):99;const trendBase=Math.max(1,prev7.reduce((s,x)=>s+x.volume,0));const trend=(acute/trendBase)-1;const prs=prRows(rows);const muscle={};rows.forEach(r=>r.muscles.forEach(m=>muscle[m]=(muscle[m]||0)+r.volume));const moduleVals=[counts.Push,counts.Pull,counts.Legs].filter(Boolean);const balance=moduleVals.length?1-(Math.max(...moduleVals)-Math.min(...moduleVals))/Math.max(...moduleVals):0;let pressure='低',pressureText='恢复压力低，可以正常推进。';if(acwr>1.45||last7Sessions>=5||(last7Sessions>=4&&last7Basket>=2)){pressure='高';pressureText='最近 7 天负荷偏高，下一次主项不要强行冲重量。'}else if(acwr>1.15||last7Sessions>=4||last7Basket>=2){pressure='中';pressureText='恢复压力中等，主项可以维持，辅助动作控制总量。'}const score=Math.round(clamp(55+strength.length*.75+balance*18+Math.max(0,Math.min(.35,trend))*25-(pressure==='高'?12:pressure==='中'?5:0),40,96));return {records,rows,cal,strength,counts,loads,total,acute,chronic,acwr,last7Sessions,last7Basket,daysSinceRest,trend,prs,muscle,balance,pressure,pressureText,score}}
  function installStyle(){if($('#v37Style'))return;const st=document.createElement('style');st.id='v37Style';st.textContent=`.v37-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.v37-wide{grid-column:1/-1}.v37-kpi{min-height:92px}.v37-kpi small{display:block;color:var(--muted);font-weight:850;font-size:11px}.v37-kpi b{display:block;font-size:22px;letter-spacing:-.04em}.v37-kpi em{font-style:normal;color:#337aa4;font-weight:900;font-size:11px}.v37-list{display:grid;gap:8px}.v37-row{display:grid;grid-template-columns:76px 1fr auto;gap:8px;align-items:center;border-radius:16px;padding:9px;background:rgba(255,255,255,.52);border:1px solid rgba(255,255,255,.68);font-size:12px;font-weight:820}.v37-track{height:10px;border-radius:999px;background:rgba(45,80,116,.10);overflow:hidden}.v37-track i{display:block;height:100%;border-radius:999px;background:linear-gradient(90deg,var(--a),var(--b))}.v37-badge{display:inline-block;padding:6px 9px;border-radius:999px;background:rgba(255,255,255,.62);border:1px solid rgba(255,255,255,.76);font-size:11px;font-weight:950;color:#2e6988}.v37-table{display:grid;gap:8px}.v37-table .v37-row{grid-template-columns:92px 1fr 58px}.v37-svg text{font-family:-apple-system,BlinkMacSystemFont,'PingFang SC',sans-serif}.v37-risk-high{color:#b33d52}.v37-risk-mid{color:#9a6418}.v37-risk-low{color:#08785e}@media(max-width:370px){.v37-grid{grid-template-columns:1fr}.v37-table .v37-row{grid-template-columns:80px 1fr 52px}}`;document.head.appendChild(st)}
  function installShell(){const s=$('#analysis');if(!s)return;if(s.dataset.v37Shell==='1')return;s.dataset.v37Shell='1';s.innerHTML=`<header class="head"><div><h1>训练分析</h1><p class="sub" id="range">数据库驱动 · V37 专业版</p></div><div class="chip" id="score">评分</div></header><article class="glass card"><div class="kicker">DATABASE ENGINE</div><p class="note" id="summary"></p><div class="tags" id="pills"></div></article><div class="metric-grid" id="stats"></div><div class="section-title"><h2>训练负荷</h2><span>日负荷 + 7日均线</span></div><article class="glass card"><div id="volChart"></div><p class="note" id="loadNote"></p></article><div class="section-title"><h2>结构平衡</h2><span>频率 / 容量 / 肌群</span></div><div class="chart-grid"><article class="glass card"><div class="chart-head"><h3>模块频率</h3><span class="note">次数</span></div><div id="freqChart"></div></article><article class="glass card"><div class="chart-head"><h3>模块训练量</h3><span class="note">kg·次</span></div><div id="loadChart"></div></article><article class="glass card v37-wide"><div class="chart-head"><h3>肌群覆盖热区</h3><span class="note">按校正训练量</span></div><div id="v37Muscle"></div></article></div><div class="section-title"><h2>主项进步</h2><span>估算 e1RM</span></div><article class="glass card"><div id="prChart"></div><div class="v37-table" id="v37PRTable"></div></article><div class="section-title"><h2>疲劳与建议</h2><span>最近7天</span></div><div class="insight-grid"><article class="glass card"><div id="v37Recovery"></div></article><article class="glass card"><div id="v37Next"></div></article></div><div class="section-title"><h2>专业解读</h2><span>指标 + 结论</span></div><div class="insight-grid" id="insights"></div>`}
  function svgLineBars(cal){const w=360,h=178,p=26;const vals=cal.map(x=>x.volume),avg=rolling(cal,7),max=Math.max(1,...vals,...avg);const step=(w-2*p)/Math.max(1,cal.length);const bars=cal.map((x,i)=>{const bh=x.volume/max*(h-2*p);const x0=p+i*step+step*.15;return `<rect x="${x0}" y="${h-p-bh}" width="${Math.max(2,step*.7)}" height="${bh}" rx="4" fill="rgba(64,207,253,.35)"/>`}).join('');const pts=avg.map((v,i)=>[p+i*step+step*.5,h-p-v/max*(h-2*p)]);return `<svg class="svg v37-svg" viewBox="0 0 ${w} ${h}" role="img" aria-label="训练负荷趋势"><defs><linearGradient id="v37g" x1="0" x2="1"><stop stop-color="#40cffd"/><stop offset="1" stop-color="#78efd0"/></linearGradient></defs>${bars}<path d="${pts.map((q,i)=>(i?'L':'M')+q[0]+','+q[1]).join(' ')}" fill="none" stroke="url(#v37g)" stroke-width="4" stroke-linecap="round"/><text x="${p}" y="16" font-size="11" fill="#708096">7日均线</text><text x="${p}" y="${h-4}" font-size="11" fill="#708096">${cal[0]?.date||''}</text><text x="${w-p}" y="${h-4}" text-anchor="end" font-size="11" fill="#708096">${cal.at(-1)?.date||''}</text></svg>`}
  function svgBars(labels,vals){const w=360,h=180,p=28,max=Math.max(1,...vals),slot=(w-2*p)/labels.length,bw=slot*.52;return `<svg class="svg v37-svg" viewBox="0 0 ${w} ${h}">${labels.map((l,i)=>{const bh=vals[i]/max*(h-62),x=p+i*slot+slot*.24,y=h-p-bh;return `<rect x="${x}" y="${y}" width="${bw}" height="${bh}" rx="10" fill="${i%2?'#78efd0':'#40cffd'}"/><text x="${x+bw/2}" y="${y-8}" text-anchor="middle" font-size="12" fill="#112136">${fmt(vals[i])}</text><text x="${x+bw/2}" y="${h-8}" text-anchor="middle" font-size="12" fill="#708096">${l}</text>`}).join('')}</svg>`}
  function barList(obj,unit=''){const entries=Object.entries(obj).filter(x=>x[1]>0).sort((a,b)=>b[1]-a[1]);const max=Math.max(1,...entries.map(x=>x[1]));return `<div class="v37-list">${entries.map(([k,v])=>`<div class="v37-row"><b>${k}</b><div class="v37-track"><i style="width:${v/max*100}%"></i></div><span>${fmt(v)}${unit}</span></div>`).join('')}</div>`}
  function render(){installStyle();installShell();const a=compute();if(!a.records.length)return;const avg=a.strength.length?a.total/a.strength.length:0;$('#range').textContent=`${a.records[0].date}–${a.records.at(-1).date} · ${V}`;$('#score').textContent=`成熟度 ${a.score}`;$('#summary').textContent=`基于 Supabase 云端档案重新计算：${a.records.length} 天记录、${a.strength.length} 个力量训练日、${fmt(a.total)} kg·次校正总训练量。双哑铃动作按 multiplier 校正，篮球和休息不计入力量负荷。`;$('#pills').innerHTML=[`力量 ${a.strength.length} 天`,`篮球 ${a.counts.Basketball} 天`,`休息 ${a.counts.Rest} 天`,`ACWR ${a.acwr.toFixed(2)}`,`最近7天 ${fmt(a.acute)} kg·次`].map(x=>`<span>${x}</span>`).join('');$('#stats').innerHTML=`<div class="metric glass v37-kpi"><small>校正总训练量</small><b>${fmt(a.total)}</b><em>kg·次</em></div><div class="metric glass v37-kpi"><small>平均力量日负荷</small><b>${fmt(avg)}</b><em>kg·次/次</em></div><div class="metric glass v37-kpi"><small>模块均衡度</small><b>${Math.round(a.balance*100)}</b><em>/100</em></div><div class="metric glass v37-kpi"><small>恢复压力</small><b class="${a.pressure==='高'?'v37-risk-high':a.pressure==='中'?'v37-risk-mid':'v37-risk-low'}">${a.pressure}</b><em>ACWR ${a.acwr.toFixed(2)}</em></div>`;$('#volChart').innerHTML=svgLineBars(a.cal);$('#loadNote').textContent=`最近7天负荷 ${fmt(a.acute)}，4周折算周均 ${fmt(a.chronic)}，急慢性负荷比 ACWR=${a.acwr.toFixed(2)}。${a.pressureText}`;$('#freqChart').innerHTML=svgBars(['推','拉','腿','篮','休'],[a.counts.Push,a.counts.Pull,a.counts.Legs,a.counts.Basketball,a.counts.Rest]);$('#loadChart').innerHTML=svgBars(['推','拉','腿'],[a.loads.Push,a.loads.Pull,a.loads.Legs]);$('#v37Muscle').innerHTML=barList(a.muscle,'');const top=a.prs.slice(0,5);$('#prChart').innerHTML=svgBars(top.map(x=>x.name.slice(0,4)),top.map(x=>Math.round(x.gain*100)));$('#v37PRTable').innerHTML=top.map(x=>`<div class="v37-row"><b>${x.name}</b><div><span class="v37-badge">首次 ${x.first.kg}kg×${x.first.reps}</span> <span class="v37-badge">最佳 ${x.best.kg}kg×${x.best.reps}</span><p class="note">最佳出现在 ${x.best.date}，最近一次 ${x.last.date} 为 ${x.last.kg}kg×${x.last.reps}。</p></div><span>${Math.round(x.gain*100)}%</span></div>`).join('');const lastStrength=[...a.records].reverse().find(r=>!['Basketball','Rest'].includes(dayCat(r)));const seq=['Push','Pull','Legs'];const lastM=lastStrength?dayCat(lastStrength):'Push';const next=seq[(seq.indexOf(lastM)+1)%3]||'Push';const recentModules=a.records.slice(-7).map(dayCat).join(' → ');$('#v37Recovery').innerHTML=`<div class="kicker">RECOVERY</div><h3 style="margin:0 0 8px">${a.pressure}恢复压力</h3><p class="note">最近7天力量 ${a.last7Sessions} 次、篮球 ${a.last7Basket} 次、距上次休息 ${a.daysSinceRest>30?'较久':a.daysSinceRest+' 天'}。${a.pressureText}</p><div class="tags"><span>ACWR ${a.acwr.toFixed(2)}</span><span>趋势 ${pct(a.trend)}</span><span>7日负荷 ${fmt(a.acute)}</span></div>`;$('#v37Next').innerHTML=`<div class="kicker">NEXT SESSION</div><h3 style="margin:0 0 8px">下一次力量：${next}</h3><p class="note">最后力量日是 ${lastStrength?.date||'—'} 的 ${lastM}。篮球和休息只影响恢复评估，不推进 P/P/L 顺序。最近7天模块链：${recentModules}。</p><div class="tags"><span>${next==='Push'?'胸肩三头':next==='Pull'?'背二头':'臀腿核心'}</span><span>${a.pressure==='高'?'保守推进':'正常推进'}</span></div>`;const imbalance=(()=>{const vals={Push:a.counts.Push,Pull:a.counts.Pull,Legs:a.counts.Legs};const max=Object.entries(vals).sort((a,b)=>b[1]-a[1])[0],min=Object.entries(vals).sort((a,b)=>a[1]-b[1])[0];return max[1]-min[1]>=3?`${max[0]} 频率明显高于 ${min[0]}，后续应优先补 ${min[0]} 或降低 ${max[0]} 辅助动作总量。`:'Push/Pull/Legs 频率差距可控，三分化结构基本成立。'})();const prText=top[0]?`${top[0].name} 是当前进步最高主项，估算 e1RM 较首次提升约 ${Math.round(top[0].gain*100)}%。`:'主项数据还需要继续累计。';$('#insights').innerHTML=[['数据可信度',`已从完整记录计算 ${a.rows.length} 个有效力量组。动作名称通过前端 catalog 再次归一化，避免“俯身飞鸟/坐姿俯身飞鸟”等重复统计。`],['负荷趋势',`最近7天对前7天趋势为 ${pct(a.trend)}。如果连续两周 > +25%，建议不要同时增加重量和组数。`],['结构均衡',imbalance],['主项进步',prText],['执行建议',`${next} 日主项先按最近稳定重量完成默认 12 次；只有最后一组速度和动作都稳定时再试小幅加重。`]].map(([h,p])=>`<div class="smart"><b>${h}</b><p>${p}</p></div>`).join('')}
  function schedule(){setTimeout(render,60)}
  document.addEventListener('click',e=>{if(e.target.closest('[data-tab="analysis"]')||e.target.closest('[data-action="cloudPull"]')||e.target.closest('[data-action="cloudPasswordLogin"]'))schedule()});
  window.addEventListener('focus',schedule);
  let sig='';setInterval(()=>{const active=$('#analysis')?.classList.contains('active');const rec=getRecords();const s=rec.length+'|'+(rec.at(-1)?.date||'')+'|'+JSON.stringify(rec).length;if(active&&(s!==sig||$('#analysis')?.dataset.v37Shell!=='1')){sig=s;render()}},1000);
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',schedule);else schedule();
})();
